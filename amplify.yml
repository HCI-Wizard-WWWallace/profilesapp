version: 1
backend:
  phases:
    build:
      commands:
        - nvm use 20
        - npm install --legacy-peer-deps
frontend:
  phases:
    build:
      commands:
        - npm run build --if-present
  artifacts:
    baseDirectory: .
    files:
      - '**/*'
